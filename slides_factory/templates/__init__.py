"""Reserved for built-in templates.

The engine ships no concrete templates (it stays brand-agnostic). The grid
itself is core (`slides_factory.layout.render.render_layout`), and templates are
authored on top via `slides_factory.templating.Template` + `@at`.
"""
